doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible" content="IE=edge")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title= title
    body
        div 
            each worker in workers
                p= worker.firstname
                p= worker.surname
                p= worker.email
                p= worker.role
                p= worker.branch
               
          

            a(href="/workers/worker-form") Manage Team
            br
            a(href="/") Home